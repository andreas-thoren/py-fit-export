# py-fit-export
Minimal python program for exporting key information from .fit files (garmin) and exporting to other file formats such as excel.

## Requirements
This project requires the Garmin FIT SDK, installed via pip:

    pip install garmin-fit-sdk

The SDK is licensed by Garmin and is not distributed with this repository.
